import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moi.teethtimer',
  appName: 'teeth_timer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
